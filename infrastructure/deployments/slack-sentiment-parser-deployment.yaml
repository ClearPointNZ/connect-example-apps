apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: slack-sentiment-parser
  namespace: sample-app
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: slack-sentiment-parser
    spec:
      containers:
      - name: slacksentimentparser
        image: slacksentimentparser:latest
        imagePullPolicy: IfNotPresent
        command:
          - java
          - -Xms128m
          - -Xmx768m
          - -jar
          - /usr/share/slacksentimentparser/app.jar
          - -P/etc/java-config/slack-sentiment-parser.properties
        ports:
        - containerPort: 8080
          protocol: TCP
        volumeMounts:
        - mountPath: /etc/java-config
          name: java-config
      volumes:
      - configMap:
          defaultMode: 420
          name: sample-app-config
        name: java-config
